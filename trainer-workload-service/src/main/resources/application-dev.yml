logging:
  level:
    root: info
    org.example.workload: info
    org.springframework.web: info

management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics,prometheus
  endpoint:
    health:
      show-details: always