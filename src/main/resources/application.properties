db.driver=org.postgresql.Driver
db.url=jdbc:postgresql://${DB_HOST}:${DB_PORT}/${POSTGRES_DB}
db.username=${POSTGRES_USER}
db.password=${POSTGRES_PASSWORD}

hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
hibernate.show_sql=false
hibernate.format_sql=false
hibernate.generate_statistics=false
hibernate.hbm2ddl.auto=validate

server.servlet.mapping=/
server.api.mapping=/api/*

interceptor.path.include=/api/**
interceptor.path.exclude=/api/v1/trainings/training-types
auth.bypass.post.paths=/api/v1/trainees,/api/v1/trainers

server.api.mapping=/api/*

swagger.ui.path=/swagger-ui/**
swagger.ui.resource.path=classpath:/META-INF/resources/webjars/swagger-ui/5.32.8/
swagger.api.docs.url=/openapi.json
swagger.api.docs.resource.path=classpath:/META-INF/resources/
swagger.html.url=/swagger-ui.html
swagger.redirect.url=/swagger-ui/index.html?url=/openapi.json


# ===================================================================
# Spring Boot Configuration - DEFAULT Configuration
# ===================================================================
# This is the entry configuration file. It sets default values
# and specifies which profile should be active by default.

# 1. Active Profile
# By default, we launch the application in the 'local' profile.
# For other environments, pass: -Dspring.profiles.active=dev
spring.profiles.active=local

# 2. General Application Information
info.app.name=Gym CRM System
info.app.description=Spring Boot Implementation of Gym CRM
info.app.version=1.0.0

# 3. Spring Boot Actuator Common Settings
# Change port for actuator endpoints to isolate them from public API (Best Practice)
management.server.port=8081
management.endpoints.enabled-by-default=true
>>>>>>> b1277a9 (Implemented integration tests for trainee, training, and user controllers, updated calls to repositories in unit tests for services)
